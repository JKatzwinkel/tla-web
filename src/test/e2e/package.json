{
  "devDependencies": {
    "cypress": "^13.8.1",
    "cypress-terminal-report": "^6.0.1",
    "eslint-plugin-cypress": "^3.0.2"
  },
  "scripts": {
    "cy": "npx cypress run",
    "lint": "npx eslint cypress/tests"
  }
}
